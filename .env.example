# Vos PY no requiere variables de entorno para funcionar.
# El catálogo se genera del CSV en build (scripts/build-products.mjs).
# Si en el futuro se agrega un backend/API, documentar aquí las variables.
# IMPORTANTE: nunca commitear el archivo .env real (está en .gitignore).
